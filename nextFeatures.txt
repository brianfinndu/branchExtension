```
final final features

- settings
  - make settings persistent with chrome.storage.sync
  - tie all page style colors to chrome.storage.sync

- tree-management
  x get all trees (unique id, name, last modified)
  x dynamically generate a card for each tree
  x include unique id in dataset for write operations
  - new tree button saves current tree to drive then makes a default tree

- dashboard
  x "connect to drive" button for interactive oauth
  - dynamically load current tree name

x active tree page
  x "save tree" button which... saves the tree to Drive
  x "rename tree" button which... renames the tree
```